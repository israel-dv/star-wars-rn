import { StyleSheet } from "react-native";

export const textRegularStyles = StyleSheet.create({
  textRegular: {
    fontFamily: "Sen-Regular",
    fontSize: 15,
    marginTop: 5,
    color: "white",
  },
});
